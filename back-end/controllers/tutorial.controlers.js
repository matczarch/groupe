const db = require("../models");
const Tutorial = db.tutorials;
const Op = db.Sequelize.Op;

//Créer et enregistrer un nouveau didacticiel
exports.create = (req, res) => {
  
};

// Récupérez tous les didacticiels de la base de données.
exports.findAll = (req, res) => {
  
};

// Trouver un seul tutoriel avec un identifiant
exports.findOne = (req, res) => {
  
};

// Mettre à jour un didacticiel par l'identifiant dans la demande
exports.update = (req, res) => {
  
};

// Supprimer un didacticiel avec l'identifiant spécifié dans la demande
exports.delete = (req, res) => {
  
};

// Supprimez tous les didacticiels de la base de données.
exports.deleteAll = (req, res) => {
  
};

// Trouver tous les tutoriels publiés
exports.findAllPublished = (req, res) => {
  
};